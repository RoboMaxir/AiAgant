# AI Content Generator - افزونه تولید محتوا با هوش مصنوعی

## 📋 توضیحات

افزونه AI Content Generator یک ابزار قدرتمند و حرفه‌ای برای تولید محتوا با استفاده از API رسمی OpenAI است. این افزونه با طراحی مدرن و امکانات پیشرفته، تجربه‌ای فوق‌العاده برای تولید انواع محتوا فراهم می‌کند.

## ✨ ویژگی‌های کلیدی

### 🔐 امنیت بالا
- استفاده از WordPress REST API
- محافظت با nonce برای تمام درخواست‌ها
- Sanitize کردن کامل تمام ورودی‌ها
- ذخیره امن کلید API

### 🎨 رابط کاربری مدرن
- طراحی واکنش‌گرا (Responsive)
- حالت تاریک و روشن
- انیمیشن‌های زیبا و روان
- تجربه کاربری بهینه

### 🤖 قابلیت‌های هوش مصنوعی
- پشتیبانی از مدل‌های مختلف OpenAI (GPT-4, GPT-4o, GPT-3.5)
- تنظیمات پیشرفته (temperature, max_tokens)
- تولید انواع محتوا (مقاله، شبکه اجتماعی، ایمیل و...)

### 📊 مدیریت کامل
- ذخیره تاریخچه تولیدات
- آمار و گزارش‌گیری
- امکان جستجو و فیلتر
- صادرات داده‌ها

## 📁 ساختار فایل‌ها

```
ai-content-generator/
├── ai-content-generator.php          # فایل اصلی افزونه
├── assets/
│   ├── css/
│   │   ├── frontend.css              # استایل‌های فرانت‌اند
│   │   └── admin.css                 # استایل‌های مدیریت
│   └── js/
│       ├── frontend.js               # جاوااسکریپت فرانت‌اند
│       └── admin.js                  # جاوااسکریپت مدیریت
├── templates/
│   ├── generator-form.php            # قالب فرم تولید
│   ├── admin-page.php                # صفحه اصلی مدیریت
│   ├── settings-page.php             # صفحه تنظیمات
│   └── history-page.php              # صفحه تاریخچه
├── languages/                        # فایل‌های ترجمه
└── README.md                         # این فایل
```

## 🚀 نصب و راه‌اندازی

### مرحله ۱: نصب افزونه
1. فایل‌ها را در پوشه `/wp-content/plugins/ai-content-generator/` کپی کنید
2. از طریق پنل مدیریت وردپرس، افزونه را فعال کنید

### مرحله ۲: دریافت کلید API
1. به [OpenAI Platform](https://platform.openai.com/) بروید
2. حساب کاربری ایجاد کرده یا وارد شوید
3. از بخش API Keys یک کلید جدید ایجاد کنید

### مرحله ۳: تنظیمات اولیه
1. در پنل مدیریت وردپرس به `AI Generator > Settings` بروید
2. کلید API خود را وارد کنید
3. مدل مورد نظر را انتخاب کنید (پیشنهاد: GPT-4o-mini)
4. تنظیمات max_tokens و temperature را تنظیم کنید
5. روی "Test API Connection" کلیک کرده و از صحت تنظیمات اطمینان یابید

## 📖 نحوه استفاده

### استفاده از شورت‌کد
برای نمایش فرم تولید محتوا در هر صفحه یا پست:

```php
[ai_generator]
```

با تنظیمات سفارشی:
```php
[ai_generator title="تولیدکننده محتوای من"]
```

### استفاده برنامه‌نویسی
```php
// دریافت نمونه کلاس
$ai_generator = AIContentGenerator::get_instance();

// تولید محتوا
$response = $ai_generator->generate_content_api(new WP_REST_Request('POST', '/generate'));
```

## ⚙️ تنظیمات

### تنظیمات پیشرفته

**Max Tokens**: تعداد حداکثر توکن‌های تولیدی (۵۰-۴۰۰۰)
- کوتاه: ۲۰۰-۵۰۰
- متوسط: ۵۰۰-۱۰۰۰  
- بلند: ۱۰۰۰-۲۰۰۰

**Temperature**: سطح خلاقیت (۰.۰-۱.۰)
- محافظه‌کارانه: ۰.۱-۰.۳
- متعادل: ۰.۵-۰.۷
- خلاق: ۰.۸-۱.۰

### انواع محتوا
- **مقاله**: مقالات جامع و تحلیلی
- **شبکه اجتماعی**: پست‌های جذاب و کوتاه
- **ایمیل**: ایمیل‌های حرفه‌ای
- **داستان**: محتوای داستانی و روایی
- **توضیحات محصول**: توضیحات بازاریابی
- **پست وبلاگ**: محتوای وبلاگ‌نویسی

## 🔧 API Endpoints

### تولید محتوا
```
POST /wp-json/ai-content-generator/v1/generate
```

پارامترها:
```json
{
    "prompt": "متن درخواستی",
    "content_type": "article|social_media|email|story|product_description|blog_post", 
    "content_length": "short|medium|long"
}
```

### دریافت تاریخچه
```
GET /wp-json/ai-content-generator/v1/history?page=1&per_page=10
```

## 🎨 سفارشی‌سازی CSS

### متغیرهای CSS
```css
:root {
    --ai-primary: #6366f1;        /* رنگ اصلی */
    --ai-secondary: #f8fafc;      /* رنگ ثانویه */
    --ai-text: #1f2937;           /* رنگ متن */
    --ai-border: #e5e7eb;         /* رنگ حاشیه */
    --ai-radius: 8px;             /* شعاع گردی */
}
```

### کلاس‌های قابل سفارشی‌سازی
- `.ai-generator-container`: کانتینر اصلی
- `.ai-form`: فرم تولید
- `.ai-result`: نتیجه تولیدی
- `.generate-btn`: دکمه تولید

## 🌍 چندزبانه‌سازی

افزونه آماده ترجمه است. برای اضافه کردن زبان جدید:

1. فایل `.pot` را از پوشه `languages` کپی کنید
2. با ابزارهای مثل Poedit ترجمه کنید
3. فایل‌های `.po` و `.mo` را در پوشه `languages` قرار دهید

## 🔒 امنیت

### نکات امنیتی مهم
- کلید API هرگز در کد ذخیره نمی‌شود
- تمام ورودی‌ها sanitize می‌شوند
- از nonce برای تمام درخواست‌ها استفاده می‌شود
- مجوزهای کاربری بررسی می‌شوند

### توصیه‌های امنیتی
- کلید API را محرمانه نگه دارید
- دسترسی‌های فایل را کنترل کنید
- به‌روزرسانی‌های افزونه را نصب کنید

## 📊 بهینه‌سازی عملکرد

### کاهش مصرف منابع
- استفاده از caching برای درخواست‌ها
- بهینه‌سازی پایگاه داده
- فشرده‌سازی CSS و JS

### توصیه‌های عملکرد
- از CDN برای فایل‌های استاتیک استفاده کنید
- تنظیمات مناسب timeout
- مدیریت حافظه برای درخواست‌های طولانی

## 🐛 عیب‌یابی

### مشکلات رایج

**خطای "API key not configured"**
- کلید API را در تنظیمات وارد کنید
- از صحت کلید اطمینان یابید

**خطای "Invalid response from OpenAI"**
- اتصال اینترنت را بررسی کنید
- وضعیت حساب OpenAI را چک کنید

**محتوا تولید نمی‌شود**
- تنظیمات max_tokens را افزایش دهید
- پیام درخواستی را ساده‌تر کنید

### فعال‌سازی debug mode
```php
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
```

## 📈 بهبودهای آتی

### نسخه‌های بعدی
- [ ] پشتیبانی از مدل‌های محلی
- [ ] قالب‌های آماده محتوا
- [ ] ادغام با Gutenberg
- [ ] API خارجی برای سایت‌های دیگر
- [ ] امکان bulk generation

### مشارکت در توسعه
برای مشارکت در بهبود افزونه:
1. مخزن را Fork کنید
2. تغییرات خود را اعمال کنید
3. Pull Request ایجاد کنید

## 📞 پشتیبانی

### اطلاعات تماس
- **ایمیل**: support@yourwebsite.com
- **وبسایت**: https://yourwebsite.com
- **مستندات**: https://docs.yourwebsite.com

### گزارش باگ
لطفاً باگ‌ها را همراه با اطلاعات زیر گزارش دهید:
- نسخه وردپرس
- نسخه PHP
- پیام خطا
- مراحل بازتولید مشکل

## 📄 مجوز

این افزونه تحت مجوز GPL v2 منتشر شده است.

---

**نکته**: این افزونه نیازمند حساب فعال OpenAI و کلید API معتبر است. هزینه‌های استفاده از API بر عهده کاربر است.